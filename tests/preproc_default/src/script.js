//? if (typeof DEBUG === 'undefined') DEBUG = false;
//? RELEASE = !DEBUG;

//? if (DEBUG) {
	console.log('This test is running in debug mode.');
//? } else {
	console.log('This test is running in release mode.');
//? }

//? if (typeof FLAG_A === 'undefined') FLAG_A = false;

//? if (FLAG_A) {
console.log('Flag A is set');
//? } else {
console.log('Flag A is not set');
//? }

//? if (typeof FLAG_B === 'undefined') FLAG_B = false;

//? if (FLAG_B) {
console.log('Flag B is set');
//? } else {
console.log('Flag B is not set');
//? }
